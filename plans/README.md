# Implementation Plans

Generated by the improve skill on 2026-07-16 (audit scope: fleet-ship +
herdr-agent-orchestration skills; commit `a4ba253`). Execute in the order
below unless dependencies say otherwise. Each executor: read the plan fully
before starting, honor its STOP conditions, and update your row when done.

Note: this repo's ax hook blocks writes on `main` - every plan works in a
git worktree and lands via PR/merge.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix three verified doc defects (monitor scoping, send semantics, fences) | P1 | S | - | DONE (5a5ff4f; send=APPEND verified live, hard rule inverted, Ctrl+U clear recipe verified; fences fixed upstream) |
| 003 | Deterministic ledger-driven teardown script | P1 | M | - | DONE (51325e4) |
| 002 | Single-source duplicated rules (SKILL 15-25% smaller) | P2 | M | 001 | TODO |
| 004 | Spike: workspace-per-fleet isolation boundary | P3 | S–M | - | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 requires 001: the dedupe must move the CORRECTED send-semantics and
  monitor texts, not the broken ones.
- 003 and 004 compose: if 004's proposal is adopted, 003's script closes
  `workspace` RES lines as the outermost teardown step (already designed in).
- Already landed outside these plans (2026-07-16, same session): the
  "Resource ledger + deterministic teardown" rule in fleet-ship SKILL.md
  Housekeeping - 003 implements its contract as a script.

## Findings considered and rejected

- Signals file in `/tmp` lost on reboot: reboot kills the panes anyway;
  ledger + orphan-sweep reconciliation covers the restart path. Not worth
  hardening.
- Cross-skill dedupe (fleet-ship vs herdr-agent-orchestration): different
  audiences legitimately restate pane mechanics; only reconcile on
  contradiction (001 handles the one found).
- Session-per-fleet backend: BLOCKED upstream - herdr subcommands cannot
  target a named session's socket (verified 2026-07-16: `--session` on
  subcommands errors, `HERDR_SOCKET` ignored). File a herdr feature request;
  004's workspace-per-fleet is the buildable middle option.
